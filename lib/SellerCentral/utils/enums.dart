enum ImageUploadStatus { inital, processing, uploaded }

enum GetApiStatus { loading, completed, error }

enum PostApiStatus { inital, loading, completed, error }

enum DeleteApiStatus { inital, deleting, successful, error }
